# Concert Ticket Booking System

> End-to-end concert ticket booking app — React + TypeScript frontend, Node.js + TypeScript backend, PostgreSQL.

---

## Quick Start

```bash

# Backend
cd backend
cp ../.env.example .env
npm install
npm run dev                      # starts on :3000

# Frontend (new terminal)
cd ../frontend
```

---

*Full architecture write-up, trade-offs, and scaling discussion coming in the final README.*